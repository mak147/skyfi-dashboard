import type { PackagePricing } from '../types';
const money=(v:number)=>new Intl.NumberFormat(undefined,{style:'currency',currency:'PKR',maximumFractionDigits:2}).format(Number(v));
export const PricingCard=({pricing}:{pricing:PackagePricing})=><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h3 className="font-semibold text-slate-900">Pricing</h3><div className="mt-4 grid gap-3 sm:grid-cols-2">{([['Monthly',pricing.monthly],['Quarterly',pricing.quarterly],['Semi annual',pricing.semi_annual],['Annual',pricing.annual]] as const).map(([l,v])=><div key={l} className="rounded-xl bg-slate-50 p-4"><p className="text-xs text-slate-500">{l}</p><p className="mt-1 font-bold text-slate-900">{money(v)}</p></div>)}</div><p className="mt-4 text-sm text-slate-600">Installation: <b>{money(pricing.installation_charge)}</b> · Tax {pricing.supports_tax?'supported':'off'} · Discounts {pricing.supports_discount?'supported':'off'}</p></section>;
