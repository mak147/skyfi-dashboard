import type { PackageBandwidth } from '../types';
const speed=(v:number|null)=>v?`${(v/1000).toLocaleString()} Mbps`:'—';
export const BandwidthCard=({bandwidth}:{bandwidth:PackageBandwidth})=><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h3 className="font-semibold text-slate-900">Bandwidth</h3><div className="mt-4 grid grid-cols-2 gap-4"><div><p className="text-xs text-slate-500">Download</p><p className="text-xl font-bold text-indigo-700">{speed(bandwidth.download_kbps)}</p></div><div><p className="text-xs text-slate-500">Upload</p><p className="text-xl font-bold text-indigo-700">{speed(bandwidth.upload_kbps)}</p></div></div><div className="mt-4 grid grid-cols-2 gap-2 text-sm text-slate-600"><p>Burst down: {speed(bandwidth.burst_download_kbps)}</p><p>Burst up: {speed(bandwidth.burst_upload_kbps)}</p><p>CIR: {speed(bandwidth.cir_kbps)}</p><p>MIR: {speed(bandwidth.mir_kbps)}</p></div><p className="mt-4 rounded-lg bg-indigo-50 p-3 text-sm font-medium text-indigo-700">{bandwidth.is_unlimited?'Unlimited data':`${Number(bandwidth.data_limit_bytes??0).toLocaleString()} bytes`}</p></section>;
