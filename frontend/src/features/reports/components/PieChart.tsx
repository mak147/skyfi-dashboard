import type { Visualization } from '../types';
const colors=['#4f46e5','#06b6d4','#f59e0b','#ef4444','#22c55e','#a855f7'];
export const PieChart=({data,title='Distribution'}:{data:Visualization;title?:string})=>{const values=data.series[0]?.data??[];const total=values.reduce((a,b)=>a+b,0);let at=0;const gradient=values.map((v,i)=>{const start=at;at+=total?v/total*100:0;return `${colors[i%colors.length]} ${start}% ${at}%`;}).join(',');return <section className="rounded-xl border border-slate-200 bg-white p-5 dark:border-slate-700 dark:bg-slate-900"><h3 className="font-semibold">{title}</h3>{total?<div className="mt-5 flex flex-col items-center gap-5 sm:flex-row"><div role="img" aria-label={`${title} pie chart`} className="h-36 w-36 shrink-0 rounded-full" style={{background:`conic-gradient(${gradient})`}}/><div className="space-y-2 text-xs">{values.slice(0,8).map((v,i)=><div key={`${data.labels[i]}-${i}`} className="flex items-center gap-2"><i className="h-2.5 w-2.5 rounded-full" style={{background:colors[i%colors.length]}}/><span>{data.labels[i]} · {(v/total*100).toFixed(1)}%</span></div>)}</div></div>:<p className="mt-6 text-sm text-slate-500">No chart data for the selected filters.</p>}</section>;};
