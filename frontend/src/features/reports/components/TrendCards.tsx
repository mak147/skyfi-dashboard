import type { Visualization } from '../types';
export const TrendCards=({data}:{data:Visualization})=><div className="grid gap-3 sm:grid-cols-2">{data.series.map(series=>{const first=series.data[0]??0,last=series.data.at(-1)??0,change=first===0?0:(last-first)/Math.abs(first)*100;return <article key={series.key} className="rounded-xl border bg-white p-4 dark:border-slate-700 dark:bg-slate-900"><p className="text-xs font-semibold text-slate-500">{series.label}</p><div className="mt-2 flex items-end justify-between"><strong className="text-xl tabular-nums">{last.toLocaleString()}</strong><span className={change>=0?'text-emerald-600':'text-red-600'}>{change>=0?'↑':'↓'} {Math.abs(change).toFixed(1)}%</span></div></article>;})}</div>;
