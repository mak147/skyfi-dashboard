import type { Kpi } from '../types';
const format=(k:Kpi)=>k.format==='currency'?`PKR ${Number(k.value).toLocaleString(undefined,{maximumFractionDigits:2})}`:Number(k.value).toLocaleString(undefined,{maximumFractionDigits:2});
export const KPICards=({items}:{items:Kpi[]})=><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{items.length?items.map(item=><article key={item.key} className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-700 dark:bg-slate-900"><p className="text-xs font-semibold uppercase tracking-wide text-slate-400">{item.label}</p><p className="mt-2 text-2xl font-bold tabular-nums text-slate-900 dark:text-white">{format(item)}</p></article>):<article className="rounded-xl border border-dashed border-slate-300 p-5 text-sm text-slate-500">No KPI values for this result.</article>}</div>;
