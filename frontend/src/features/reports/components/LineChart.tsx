import type { Visualization } from '../types';
const colors=['#4f46e5','#0891b2','#f59e0b'];
export const LineChart=({data,title='Trend'}:{data:Visualization;title?:string})=>{const values=data.series.flatMap(s=>s.data);const max=Math.max(1,...values);const width=640,height=220,pad=28;return <section className="rounded-xl border border-slate-200 bg-white p-5 dark:border-slate-700 dark:bg-slate-900"><h3 className="font-semibold">{title}</h3>{values.length?<><svg viewBox={`0 0 ${width} ${height}`} role="img" aria-label={`${title} line chart`} className="mt-4 h-56 w-full overflow-visible"><path d={`M${pad} ${height-pad}H${width-pad}M${pad} ${pad}V${height-pad}`} stroke="#94a3b8" fill="none"/>{data.series.map((series,index)=>{const points=series.data.map((value,i)=>`${pad+i*((width-pad*2)/Math.max(1,series.data.length-1))},${height-pad-(value/max)*(height-pad*2)}`).join(' ');return <polyline key={series.key} points={points} fill="none" stroke={colors[index%colors.length]} strokeWidth="3" strokeLinejoin="round"/>;})}</svg><div className="flex flex-wrap gap-4 text-xs">{data.series.map((s,i)=><span key={s.key} className="flex items-center gap-2"><i className="h-2.5 w-2.5 rounded-full" style={{background:colors[i%colors.length]}}/>{s.label}</span>)}</div></>:<p className="mt-6 text-sm text-slate-500">No chart data for the selected filters.</p>}</section>;};
