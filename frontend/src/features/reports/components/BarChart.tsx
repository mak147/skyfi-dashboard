import type { Visualization } from '../types';
export const BarChart=({data,title='Breakdown'}:{data:Visualization;title?:string})=>{const series=data.series[0];const max=Math.max(1,...(series?.data??[]));return <section className="rounded-xl border border-slate-200 bg-white p-5 dark:border-slate-700 dark:bg-slate-900"><h3 className="font-semibold">{title}</h3><div className="mt-5 space-y-3">{series?.data.length?series.data.slice(0,12).map((value,i)=><div key={`${data.labels[i]}-${i}`}><div className="flex justify-between gap-4 text-xs"><span className="truncate">{data.labels[i]||`Item ${i+1}`}</span><strong className="tabular-nums">{value.toLocaleString()}</strong></div><div className="mt-1 h-2.5 rounded-full bg-slate-100 dark:bg-slate-800"><div className="h-full rounded-full bg-indigo-500" style={{width:`${Math.max(2,value/max*100)}%`}}/></div></div>):<p className="text-sm text-slate-500">No chart data for the selected filters.</p>}</div></section>;};
