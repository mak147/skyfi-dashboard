import type { PaginationMeta,ReportColumn,ReportValue } from '../types';
const value=(v:ReportValue,type:string)=>{if(v===null||v==='')return '—';if(type==='currency')return Number(v).toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2});if(type==='number')return Number(v).toLocaleString();if(type==='date')return new Date(String(v)).toLocaleDateString();return String(v);};
export const ReportTable=({columns,rows,meta,onPage}:{columns:ReportColumn[];rows:Array<Record<string,ReportValue>>;meta?:PaginationMeta;onPage?:(page:number)=>void})=><section className="overflow-hidden rounded-xl border border-slate-200 bg-white dark:border-slate-700 dark:bg-slate-900"><div className="overflow-x-auto"><table className="min-w-full divide-y divide-slate-200 text-sm dark:divide-slate-700"><thead className="bg-slate-50 dark:bg-slate-800"><tr>{columns.map(c=><th key={c.key} className="whitespace-nowrap px-4 py-3 text-left text-xs font-semibold uppercase tracking-wide text-slate-500">{c.label}</th>)}</tr></thead><tbody className="divide-y divide-slate-100 dark:divide-slate-800">{rows.map((row,i)=><tr key={i} className="hover:bg-slate-50 dark:hover:bg-slate-800/60">{columns.map(c=><td key={c.key} className={`whitespace-nowrap px-4 py-3 ${['number','currency'].includes(c.type)?'text-right tabular-nums':''}`}>{value(row[c.key]??null,c.type)}</td>)}</tr>)}</tbody></table>{!rows.length?<div className="p-10 text-center text-sm text-slate-500">No records match the selected filters.</div>:null}</div>{meta?<footer className="flex items-center justify-between border-t px-4 py-3 text-sm dark:border-slate-700"><span>{meta.total.toLocaleString()} records · Page {meta.current_page} of {meta.last_page}</span><div className="flex gap-2"><button className="rounded-lg border px-3 py-1.5 disabled:opacity-40" disabled={meta.current_page<=1} onClick={()=>onPage?.(meta.current_page-1)}>Previous</button><button className="rounded-lg border px-3 py-1.5 disabled:opacity-40" disabled={meta.current_page>=meta.last_page} onClick={()=>onPage?.(meta.current_page+1)}>Next</button></div></footer>:null}</section>;
