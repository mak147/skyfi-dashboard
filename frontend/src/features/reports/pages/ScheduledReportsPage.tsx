import { Alert } from '@/components/ui/alert';import { apiErrorMessage } from '@/lib/apiClient';import { useSchedules } from '../api/useReports';import { ReportSkeleton } from '../components/ReportSkeleton';
export const ScheduledReportsPage=()=>{const q=useSchedules();if(q.isLoading)return <ReportSkeleton/>;return <div className="space-y-6"><header><p className="text-xs font-bold uppercase tracking-wider text-indigo-600">Schedule-ready placeholder</p><h1 className="text-2xl font-bold">Scheduled Reports</h1><p className="text-sm text-slate-500">Definitions can be prepared now. Automatic execution and delivery are reserved for a future worker.</p></header><aside className="rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900 dark:border-amber-900 dark:bg-amber-950/30 dark:text-amber-200"><strong>Scheduling is not active.</strong> No report will run or be emailed automatically in this release.</aside>{q.error?<Alert variant="danger" title="Schedules unavailable">{apiErrorMessage(q.error)}</Alert>:null}<div className="overflow-hidden rounded-xl border bg-white dark:border-slate-700 dark:bg-slate-900">{q.data?.map(s=><div key={s.id} className="flex flex-col gap-2 border-b p-4 last:border-0 dark:border-slate-700 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="font-semibold">{s.name}</h2><p className="text-xs text-slate-500">{s.frequency} · {s.export_format.toUpperCase()}</p></div><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold capitalize dark:bg-slate-800">{s.status}</span></div>)}{!q.data?.length?<div className="p-10 text-center text-sm text-slate-500">No schedule definitions have been created.</div>:null}</div></div>;};
