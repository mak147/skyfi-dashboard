import{useSearchParams}from'react-router-dom';import{Link}from'react-router-dom';import{useWorkOrders}from'../api/useFieldService';import{StatusBadge}from'../components/StatusBadge';export const FieldActivityPage=()=>{const[sp,setSp]=useSearchParams();const status=sp.get('status')??'in_progress';const q=useWorkOrders({'page[size]':50,'filter[status]':status});return <div className="space-y-5"><header><p className="text-xs font-bold uppercase tracking-wider text-indigo-600">Mobile field workspace</p><h1 className="text-2xl font-bold">Field Activity</h1><p className="text-sm text-slate-500">Check visits, materials, logs, customer evidence, and job completion.</p></header><div className="flex gap-2">{['scheduled','in_progress','on_hold'].map(x=><button key={x} className={`rounded-full px-4 py-2 text-sm font-semibold ${status===x?'bg-indigo-600 text-white':'bg-white text-slate-600 dark:bg-slate-800'}`} onClick={()=>setSp({status:x})}>{x.replaceAll('_',' ')}</button>)}</div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{q.isLoading?Array.from({length:6}).map((_,i)=><div key={i} className="h-52 animate-pulse rounded-xl bg-slate-200"/>):q.data?.data.map(({attributes:o})=><Link key={o.id} to={`/field-service/work-orders/${o.id}`} className="rounded-xl border bg-white p-5 shadow-sm transition hover:-translate-y-0.5 hover:shadow-md dark:border-slate-700 dark:bg-slate-900"><div className="flex justify-between"><span className="font-mono text-xs font-bold text-indigo-600">{o.work_order_number}</span><StatusBadge status={o.status}/></div><h2 className="mt-3 font-semibold">{o.customer_name}</h2><p className="text-sm">{o.title}</p><p className="mt-3 text-sm text-slate-500">{o.service_address}</p><div className="mt-4 border-t pt-3 text-xs text-slate-400">{o.customer_phone??'No customer phone'} · {o.technician_name??'Unassigned'}</div></Link>)}</div></div>};
