import type{TimelineItem}from'../types';export const WorkOrderTimeline=({items=[]}:{items?:TimelineItem[]})=><ol className="space-y-0">{items.length?items.map(x=><li key={x.id} className="relative border-l-2 border-indigo-100 pb-6 pl-6 last:pb-0 dark:border-indigo-900"><span className="absolute -left-2 top-1 h-3.5 w-3.5 rounded-full border-2 border-white bg-indigo-500"/><p className="text-sm font-semibold capitalize">{x.event_type.replaceAll('_',' ')}</p><p className="mt-1 text-sm text-slate-600 dark:text-slate-300">{x.description}</p><p className="mt-1 text-xs text-slate-400">{x.actor_name??'System'} · {new Date(x.created_at).toLocaleString()}</p></li>):<li className="text-sm text-slate-500">No activity recorded.</li>}</ol>;
