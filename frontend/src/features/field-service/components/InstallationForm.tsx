import{zodResolver}from'@hookform/resolvers/zod';import{useForm}from'react-hook-form';import{Button}from'@/components/ui/button';import{Alert}from'@/components/ui/alert';import{apiErrorMessage}from'@/lib/apiClient';import{installationSchema}from'../schemas';import type{InstallationFormValues}from'../types';import{useCreateRequest}from'../api/useFieldService';
const field='w-full rounded-md border border-slate-300 bg-white px-3 py-2 text-sm dark:border-slate-600 dark:bg-slate-800';export const InstallationForm=({onSuccess}:{onSuccess?:()=>void})=>{const mutation=useCreateRequest();const{register,handleSubmit,formState:{errors}}=useForm<InstallationFormValues>({resolver:zodResolver(installationSchema),mode:'onTouched',defaultValues:{customer_id:0,connection_id:0,priority:'normal',service_address:'',notes:''}});const submit=handleSubmit(async d=>{await mutation.mutateAsync(d);onSuccess?.()});return <form className="space-y-5" onSubmit={e=>void submit(e)}>{mutation.error?<Alert variant="danger" title="Unable to create request">{apiErrorMessage(mutation.error)}</Alert>:null}<div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium">Customer ID<input className={field} type="number" {...register('customer_id')}/><span className="text-xs text-red-600">{errors.customer_id?.message}</span></label><label className="text-sm font-medium">Connection ID<input className={field} type="number" {...register('connection_id')}/><span className="text-xs text-red-600">{errors.connection_id?.message}</span></label><label className="text-sm font-medium">Priority<select className={field} {...register('priority')}><option value="low">Low</option><option value="normal">Normal</option><option value="high">High</option><option value="urgent">Urgent</option></select></label><label className="text-sm font-medium">Preferred start<input className={field} type="datetime-local" {...register('preferred_start_at')}/></label><label className="text-sm font-medium sm:col-span-2">Service address<input className={field} {...register('service_address')}/><span className="text-xs text-red-600">{errors.service_address?.message}</span></label><label className="text-sm font-medium sm:col-span-2">Notes<textarea className={field} rows={3} {...register('notes')}/></label></div><div className="flex justify-end"><Button type="submit" disabled={mutation.isPending}>{mutation.isPending?'Creating…':'Create installation request'}</Button></div></form>};
