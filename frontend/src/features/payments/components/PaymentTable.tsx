import{useNavigate}from'react-router-dom';import type{Payment}from'../types';import{PaymentStatusBadge}from'./PaymentStatusBadge';export const PaymentTable=({payments,sort,onSort}:{payments:Payment[];sort:string;onSort:(s:string)=>void})=>{const n=useNavigate();const head=(key:string,label:string)=><button className="font-semibold" onClick={()=>onSort(sort===key?`-${key}`:key)}>{label}</button>;return <div className="overflow-x-auto rounded-xl border border-slate-200 bg-white shadow-sm"><table className="min-w-full"><thead className="bg-slate-50 text-left text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-4 py-3">Payment / Receipt</th><th className="px-4 py-3">Customer</th><th className="px-4 py-3">Method</th><th className="px-4 py-3">{head('payment_date','Date')}</th><th className="px-4 py-3 text-right">{head('amount','Amount')}</th><th className="px-4 py-3">Status</th></tr></thead><tbody className="divide-y divide-slate-100">{payments.map(p=><tr key={p.id} className="cursor-pointer hover:bg-slate-50" onClick={()=>n(`/payments/${p.id}`)}><td className="px-4 py-3"><p className="text-sm font-semibold text-indigo-600">{p.payment_number}</p><p className="text-xs text-slate-400">{p.receipt_number??'No receipt'}</p></td><td className="px-4 py-3"><p className="text-sm font-medium">{p.customer_name}</p><p className="text-xs text-slate-400">{p.customer_code}</p></td><td className="px-4 py-3 text-sm">{p.payment_method_name}</td><td className="px-4 py-3 text-sm">{new Date(p.payment_date).toLocaleString()}</td><td className="px-4 py-3 text-right text-sm font-bold tabular-nums">{p.currency} {Number(p.amount).toLocaleString()}</td><td className="px-4 py-3"><PaymentStatusBadge status={p.status}/></td></tr>)}</tbody></table>{payments.length===0&&<p className="p-12 text-center text-sm text-slate-500">No payments match these filters.</p>}</div>};
