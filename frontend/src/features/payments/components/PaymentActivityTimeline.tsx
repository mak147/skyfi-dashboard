import type{PaymentActivity}from'../types';export const PaymentActivityTimeline=({activities}:{activities:PaymentActivity[]})=><ol className="space-y-4">{activities.map(a=><li key={a.id} className="relative border-l-2 border-indigo-100 pl-5"><span className="absolute -left-1.5 top-1 h-3 w-3 rounded-full bg-indigo-500"/><p className="text-sm font-semibold capitalize">{a.action.replace('_',' ')}</p><p className="text-sm text-slate-500">{a.description}</p><p className="mt-1 text-xs text-slate-400">{a.performed_by_name??'System'} · {new Date(a.created_at).toLocaleString()}</p></li>)}{activities.length===0&&<p className="text-sm text-slate-500">No activity recorded.</p>}</ol>;
