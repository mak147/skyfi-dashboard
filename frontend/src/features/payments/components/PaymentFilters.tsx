import type{PaymentFilters as F,PaymentMethod}from'../types';import{PaymentSearch}from'./PaymentSearch';export const PaymentFilters=({filters,methods,onChange}:{filters:F;methods:PaymentMethod[];onChange:(f:F)=>void})=><section className="grid gap-3 rounded-xl border border-slate-200 bg-white p-4 shadow-sm md:grid-cols-5" aria-label="Payment filters"><div className="md:col-span-2"><PaymentSearch value={filters.search??''} onChange={search=>onChange({...filters,search})}/></div><select aria-label="Status" className="rounded-md border border-slate-300 px-3 text-sm" value={filters.status??''} onChange={e=>onChange({...filters,status:e.target.value})}><option value="">All statuses</option>{['pending','completed','partially_applied','failed','cancelled','refunded'].map(x=><option key={x} value={x}>{x.replace('_',' ')}</option>)}</select><select aria-label="Payment method" className="rounded-md border border-slate-300 px-3 text-sm" value={filters.payment_method??''} onChange={e=>onChange({...filters,payment_method:e.target.value})}><option value="">All methods</option>{methods.map(m=><option key={m.id} value={m.code}>{m.name}</option>)}</select><div className="flex gap-2"><input aria-label="From date" type="date" className="min-w-0 rounded-md border border-slate-300 px-2 text-sm" value={filters.date_from??''} onChange={e=>onChange({...filters,date_from:e.target.value})}/><input aria-label="To date" type="date" className="min-w-0 rounded-md border border-slate-300 px-2 text-sm" value={filters.date_to??''} onChange={e=>onChange({...filters,date_to:e.target.value})}/></div></section>;
