import{useNavigate}from'react-router-dom';import type{Payment}from'../types';import{PaymentStatusBadge}from'./PaymentStatusBadge';export const PaymentCard=({payment}:{payment:Payment})=>{const n=useNavigate();return <button type="button" onClick={()=>n(`/payments/${payment.id}`)} className="w-full rounded-xl border border-slate-200 bg-white p-4 text-left shadow-sm"><div className="flex justify-between"><span className="text-xs font-bold text-indigo-600">{payment.payment_number}</span><PaymentStatusBadge status={payment.status}/></div><p className="mt-3 font-semibold text-slate-900">{payment.customer_name}</p><p className="text-sm text-slate-500">{payment.payment_method_name} · {payment.payment_date.slice(0,10)}</p><p className="mt-3 text-xl font-bold tabular-nums">{payment.currency} {Number(payment.amount).toLocaleString()}</p></button>};
