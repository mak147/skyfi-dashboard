import{useNavigate,useParams}from'react-router-dom';import{useMutation,useQuery,useQueryClient}from'@tanstack/react-query';import{Alert}from'@/components/ui/alert';import{Button}from'@/components/ui/button';import{apiErrorMessage}from'@/lib/apiClient';import{getPayment,refundPayment}from'../api/paymentApi';import{RefundForm,type RefundValues}from'../components/RefundForm';export const RefundPaymentPage=()=>{const{id}=useParams();const n=useNavigate();const qc=useQueryClient();const q=useQuery({queryKey:['payment',id],queryFn:()=>getPayment(Number(id))});const m=useMutation({mutationFn:(v:RefundValues)=>refundPayment(Number(id),v),onSuccess:()=>{void qc.invalidateQueries({queryKey:['payments']});void qc.invalidateQueries({queryKey:['payment',id]});void qc.invalidateQueries({queryKey:['invoices']});n(`/payments/${id}`)}});if(q.isLoading)return <div className="h-80 animate-pulse rounded-xl bg-slate-100"/>;if(!q.data)return <Alert title="Payment unavailable">{apiErrorMessage(q.error)}</Alert>;return <div className="mx-auto max-w-2xl space-y-6"><Button variant="ghost" onClick={()=>n(`/payments/${id}`)}>← Back</Button><div><h1 className="text-2xl font-bold">Refund Payment</h1><p className="text-sm text-slate-500">{q.data.payment_number} · {q.data.customer_name}</p></div>{m.error&&<Alert title="Refund failed">{apiErrorMessage(m.error)}</Alert>}<section className="rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><RefundForm max={Number(q.data.amount)-Number(q.data.refunded_amount)} onSubmit={v=>m.mutate(v)} isLoading={m.isPending}/></section></div>};
