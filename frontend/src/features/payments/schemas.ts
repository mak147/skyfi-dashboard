import{z}from'zod';export const paymentSchema=z.object({customer_id:z.number().int().positive(),connection_id:z.number().int().positive().optional(),payment_method_id:z.number().int().positive(),payment_account_id:z.number().int().positive(),amount:z.string().refine(v=>Number(v)>0,'Amount must be greater than zero.'),tax_amount:z.string(),discount_amount:z.string(),adjustment_amount:z.string(),payment_date:z.string().min(1),reference_number:z.string().max(150),notes:z.string().max(5000),allocations:z.array(z.object({invoice_id:z.number().positive().optional(),allocation_type:z.enum(['invoice','advance_balance','credit_balance']),amount:z.string().refine(v=>Number(v)>0,'Enter an amount.'),notes:z.string().optional()}))});export const refundSchema=z.object({amount:z.string().refine(v=>Number(v)>0,'Amount must be greater than zero.'),reason:z.string().trim().min(1,'Reason is required.'),notes:z.string().optional(),reference_number:z.string().optional()});
