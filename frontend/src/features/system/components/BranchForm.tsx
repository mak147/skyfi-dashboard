import { useEffect, useState } from 'react';
import { Input } from '@/components/ui/input';
import { Button } from '@/components/ui/button';
import type { Branch } from '../types';
export const BranchForm = ({ branch, onSave, isSaving }: { branch?: Branch | null; onSave: (data: Partial<Branch>) => void; isSaving?: boolean }) => { const [form, setForm] = useState<Partial<Branch>>({ code: '', name: '', status: 'active' }); useEffect(() => setForm(branch ?? { code: '', name: '', status: 'active' }), [branch]); const set = (key: keyof Branch, value: string) => setForm((current) => ({ ...current, [key]: value })); return <form className="grid gap-3 rounded-xl border bg-white p-5 shadow-sm dark:border-slate-700 dark:bg-slate-900 md:grid-cols-2" onSubmit={(e) => { e.preventDefault(); onSave(form); }}><Input placeholder="Code" value={form.code ?? ''} onChange={(e) => set('code', e.target.value)} /><Input placeholder="Name" value={form.name ?? ''} onChange={(e) => set('name', e.target.value)} /><Input placeholder="City" value={form.city ?? ''} onChange={(e) => set('city', e.target.value)} /><Input placeholder="Email" value={form.email ?? ''} onChange={(e) => set('email', e.target.value)} /><Input placeholder="Phone" value={form.phone ?? ''} onChange={(e) => set('phone', e.target.value)} /><Input placeholder="Timezone" value={form.timezone ?? ''} onChange={(e) => set('timezone', e.target.value)} /><div className="md:col-span-2"><Button type="submit" isLoading={isSaving}>Save branch</Button></div></form>; };
