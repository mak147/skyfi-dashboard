import type { NotificationSettings } from '../types';
export const NotificationPreferenceForm = ({ value, onChange }: { value: NotificationSettings; onChange: (next: NotificationSettings) => void }) => <div className="grid gap-3 sm:grid-cols-3">{(['email_enabled', 'sms_enabled', 'in_app_enabled'] as const).map((key) => <label key={key} className="flex items-center gap-3 rounded-lg border p-3 dark:border-slate-700"><input type="checkbox" checked={Boolean(value[key])} onChange={(event) => onChange({ ...value, [key]: event.target.checked ? 1 : 0 })} /><span className="text-sm font-semibold capitalize">{key.replace('_enabled', '').replace('_', ' ')}</span></label>)}</div>;
