export const ThemeSelector = ({ value, onChange }: { value: string; onChange: (value: string) => void }) => <div className="grid gap-3 sm:grid-cols-3">{['light', 'dark', 'system'].map((theme) => <button key={theme} type="button" onClick={() => onChange(theme)} className={`rounded-xl border p-4 text-left font-semibold capitalize ${value === theme ? 'border-indigo-500 bg-indigo-50 text-indigo-700' : 'border-slate-200 bg-white text-slate-700 dark:border-slate-700 dark:bg-slate-900 dark:text-slate-100'}`}>{theme}<span className="mt-1 block text-xs font-normal text-slate-500">{theme === 'system' ? 'Follow device preference' : `${theme} interface`}</span></button>)}</div>;
