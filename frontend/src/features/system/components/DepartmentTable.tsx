import { Button } from '@/components/ui/button';
import type { Department } from '../types';
export const DepartmentTable = ({ departments, onEdit, onToggle, onDelete }: { departments: Department[]; onEdit: (item: Department) => void; onToggle: (item: Department) => void; onDelete: (id: number) => void }) => <div className="overflow-hidden rounded-xl border bg-white shadow-sm dark:border-slate-700 dark:bg-slate-900"><table className="min-w-full divide-y divide-slate-200 text-sm dark:divide-slate-700"><thead className="bg-slate-50 dark:bg-slate-800"><tr><th className="px-4 py-3 text-left">Code</th><th className="px-4 py-3 text-left">Name</th><th className="px-4 py-3 text-left">Branch</th><th className="px-4 py-3 text-left">Status</th><th className="px-4 py-3 text-right">Actions</th></tr></thead><tbody className="divide-y divide-slate-100 dark:divide-slate-800">{departments.map((department) => <tr key={department.id}><td className="px-4 py-3 font-mono">{department.code}</td><td className="px-4 py-3 font-semibold">{department.name}</td><td className="px-4 py-3 text-slate-500">{department.branch_id || 'Global'}</td><td className="px-4 py-3"><span className="rounded-full bg-indigo-50 px-2 py-1 text-xs font-semibold text-indigo-700">{department.status}</span></td><td className="space-x-2 px-4 py-3 text-right"><Button size="sm" variant="secondary" onClick={() => onEdit(department)}>Edit</Button><Button size="sm" variant="ghost" onClick={() => onToggle(department)}>{department.status === 'active' ? 'Deactivate' : 'Activate'}</Button><Button size="sm" variant="danger" onClick={() => onDelete(department.id)}>Delete</Button></td></tr>)}</tbody></table></div>;
