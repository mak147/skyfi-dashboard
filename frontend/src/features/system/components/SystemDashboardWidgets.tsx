import type { SystemDashboard } from '../types';
export const SystemDashboardWidgets = ({ data }: { data: SystemDashboard }) => <div className="grid gap-4 md:grid-cols-4">{[{ label: 'Active Branches', value: data.active_branches }, { label: 'Active Departments', value: data.active_departments }, { label: 'System Health', value: data.system_health.status }, { label: 'Configuration', value: data.configuration_status.complete ? 'Complete' : 'Review' }].map((item) => <section key={item.label} className="rounded-xl border bg-white p-5 shadow-sm dark:border-slate-700 dark:bg-slate-900"><p className="text-xs font-semibold uppercase tracking-wide text-slate-400">{item.label}</p><p className="mt-2 text-2xl font-bold text-slate-900 dark:text-white">{item.value}</p></section>)}</div>;
