import { useEffect, useState } from 'react';
import { Input } from '@/components/ui/input';
import { Button } from '@/components/ui/button';
import type { Department } from '../types';
export const DepartmentForm = ({ department, onSave, isSaving }: { department?: Department | null; onSave: (data: Partial<Department>) => void; isSaving?: boolean }) => { const [form, setForm] = useState<Partial<Department>>({ code: '', name: '', status: 'active' }); useEffect(() => setForm(department ?? { code: '', name: '', status: 'active' }), [department]); const set = (key: keyof Department, value: string) => setForm((current) => ({ ...current, [key]: key === 'branch_id' ? Number(value) || null : value })); return <form className="grid gap-3 rounded-xl border bg-white p-5 shadow-sm dark:border-slate-700 dark:bg-slate-900 md:grid-cols-2" onSubmit={(e) => { e.preventDefault(); onSave(form); }}><Input placeholder="Code" value={form.code ?? ''} onChange={(e) => set('code', e.target.value)} /><Input placeholder="Name" value={form.name ?? ''} onChange={(e) => set('name', e.target.value)} /><Input placeholder="Branch ID (optional)" value={form.branch_id ?? ''} onChange={(e) => set('branch_id', e.target.value)} /><Input placeholder="Description" value={form.description ?? ''} onChange={(e) => set('description', e.target.value)} /><div className="md:col-span-2"><Button type="submit" isLoading={isSaving}>Save department</Button></div></form>; };
