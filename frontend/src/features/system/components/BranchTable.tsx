import { Button } from '@/components/ui/button';
import type { Branch } from '../types';
export const BranchTable = ({ branches, onEdit, onToggle, onDelete }: { branches: Branch[]; onEdit: (item: Branch) => void; onToggle: (item: Branch) => void; onDelete: (id: number) => void }) => <div className="overflow-hidden rounded-xl border bg-white shadow-sm dark:border-slate-700 dark:bg-slate-900"><table className="min-w-full divide-y divide-slate-200 text-sm dark:divide-slate-700"><thead className="bg-slate-50 dark:bg-slate-800"><tr><th className="px-4 py-3 text-left">Code</th><th className="px-4 py-3 text-left">Name</th><th className="px-4 py-3 text-left">City</th><th className="px-4 py-3 text-left">Status</th><th className="px-4 py-3 text-right">Actions</th></tr></thead><tbody className="divide-y divide-slate-100 dark:divide-slate-800">{branches.map((branch) => <tr key={branch.id}><td className="px-4 py-3 font-mono">{branch.code}</td><td className="px-4 py-3 font-semibold">{branch.name}</td><td className="px-4 py-3 text-slate-500">{branch.city || '—'}</td><td className="px-4 py-3"><span className="rounded-full bg-emerald-50 px-2 py-1 text-xs font-semibold text-emerald-700">{branch.status}</span></td><td className="space-x-2 px-4 py-3 text-right"><Button size="sm" variant="secondary" onClick={() => onEdit(branch)}>Edit</Button><Button size="sm" variant="ghost" onClick={() => onToggle(branch)}>{branch.status === 'active' ? 'Deactivate' : 'Activate'}</Button><Button size="sm" variant="danger" onClick={() => onDelete(branch.id)}>Delete</Button></td></tr>)}</tbody></table></div>;
