import { useMutation } from '@tanstack/react-query';import { useNavigate } from 'react-router-dom';import { Alert } from '@/components/ui/alert';import { apiErrorMessage } from '@/lib/apiClient';import { createTicket } from '../api/supportApi';import { TicketForm } from '../components/TicketForm';
export const CreateTicketPage=()=>{const nav=useNavigate();const m=useMutation({mutationFn:createTicket,onSuccess:t=>nav(`/support/tickets/${t.id}`)});return <div className="mx-auto max-w-5xl space-y-5"><header><h1 className="text-2xl font-bold text-slate-900">Create Support Ticket</h1><p className="mt-1 text-sm text-slate-500">Connect the issue to the subscriber and relevant network context.</p></header>{m.error?<Alert variant="danger" title="Ticket could not be created">{apiErrorMessage(m.error)}</Alert>:null}<TicketForm onSubmit={v=>m.mutate(v)} isLoading={m.isPending}/></div>};
