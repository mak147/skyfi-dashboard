import { useQuery } from '@tanstack/react-query';import { Alert } from '@/components/ui/alert';import { apiErrorMessage } from '@/lib/apiClient';import { getSlaDashboard } from '../api/supportApi';import { SupportDashboardSkeleton } from '../components/SupportDashboardSkeleton';
export const SLADashboardPage=()=>{const q=useQuery({queryKey:['support','sla-dashboard'],queryFn:getSlaDashboard});if(q.isLoading)return <SupportDashboardSkeleton/>;if(!q.data)return <Alert variant="danger" title="SLA dashboard unavailable">{apiErrorMessage(q.error)}</Alert>;const s=q.data.summary;return <div className="space-y-6"><header><p className="text-xs font-semibold uppercase tracking-wider text-indigo-600">Helpdesk performance</p><h1 className="text-2xl font-bold text-slate-900">SLA Dashboard</h1></header><div className="grid grid-cols-2 gap-4 lg:grid-cols-4">{[['Response overdue',s.response_overdue],['Resolution overdue',s.resolution_overdue],['Avg. response (min)',s.avg_response_minutes],['Avg. resolution (min)',s.avg_resolution_minutes]].map(([l,v])=><div key={String(l)} className="rounded-xl border bg-white p-5 shadow-sm"><p className="text-xs uppercase text-slate-400">{l}</p><p className="mt-2 text-3xl font-bold text-slate-900">{v??0}</p></div>)}</div><section className="overflow-hidden rounded-xl border bg-white shadow-sm"><div className="border-b p-5"><h2 className="font-semibold text-slate-900">Active SLA policies</h2></div><div className="overflow-x-auto"><table className="min-w-full text-sm"><thead className="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th className="px-5 py-3">Policy</th><th className="px-5 py-3">Priority</th><th className="px-5 py-3">Response</th><th className="px-5 py-3">Resolution</th></tr></thead><tbody className="divide-y">{q.data.policies.map(p=><tr key={p.id}><td className="px-5 py-3 font-medium">{p.name}</td><td className="px-5 py-3 capitalize">{p.priority}</td><td className="px-5 py-3">{p.response_minutes} min</td><td className="px-5 py-3">{p.resolution_minutes} min</td></tr>)}</tbody></table></div></section></div>};
