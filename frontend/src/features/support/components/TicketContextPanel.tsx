import type { TicketDetail } from '../types';
export const TicketContextPanel=({detail}:{detail:TicketDetail})=><aside className="space-y-4">{Object.entries(detail.context).map(([key,value])=>value&&Object.keys(value).length?<section key={key} className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><h3 className="text-xs font-semibold uppercase tracking-wider text-indigo-600">{key.replaceAll('_',' ')}</h3><dl className="mt-3 space-y-2">{Object.entries(value).filter(([,v])=>v!==null&&v!=='').map(([field,v])=><div key={field} className="flex justify-between gap-3 text-xs"><dt className="capitalize text-slate-400">{field.replaceAll('_',' ')}</dt><dd className="max-w-[60%] text-right font-medium text-slate-700">{String(v)}</dd></div>)}</dl></section>:null)}</aside>;
