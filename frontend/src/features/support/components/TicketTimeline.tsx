import type { TimelineItem } from '../types';
export const TicketTimeline=({items}:{items:TimelineItem[]})=><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-semibold text-slate-900">Timeline</h2><ol className="mt-5 space-y-0">{items.map((item,i)=><li key={`${item.item_kind}-${item.id}`} className="relative flex gap-4 pb-6"><div className="relative z-10 mt-1 h-3 w-3 shrink-0 rounded-full bg-indigo-500 ring-4 ring-indigo-50"/>{i<items.length-1?<div className="absolute left-[5px] top-4 h-full w-px bg-slate-200"/>:null}<div><div className="flex flex-wrap items-center gap-2"><span className="text-sm font-semibold capitalize text-slate-800">{item.type.replaceAll('_',' ')}</span><time className="text-xs text-slate-400">{new Date(item.created_at).toLocaleString()}</time></div><p className="mt-1 whitespace-pre-wrap text-sm text-slate-600">{item.body}</p></div></li>)}</ol></section>;
