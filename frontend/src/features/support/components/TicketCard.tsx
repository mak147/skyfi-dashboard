import { Link } from 'react-router-dom';import type { SupportTicket } from '../types';import { PriorityBadge } from './PriorityBadge';import { TicketStatusBadge } from './TicketStatusBadge';import { SLAIndicator } from './SLAIndicator';
export const TicketCard=({ticket}:{ticket:SupportTicket})=><Link to={`/support/tickets/${ticket.id}`} className="block rounded-xl border border-slate-200 bg-white p-4 shadow-sm transition hover:border-indigo-300"><div className="flex items-center justify-between"><span className="font-mono text-xs font-semibold text-indigo-600">{ticket.ticket_number}</span><PriorityBadge priority={ticket.priority}/></div><h3 className="mt-3 font-semibold text-slate-900">{ticket.subject}</h3><p className="mt-1 text-sm text-slate-500">{ticket.customer_name}</p><div className="mt-4 flex items-center justify-between"><TicketStatusBadge status={ticket.status}/><SLAIndicator ticket={ticket}/></div></Link>;
