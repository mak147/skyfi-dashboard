import type { Category,TicketFilters } from '../types';
export const TicketFiltersBar=({filters,categories,onChange}:{filters:TicketFilters;categories:Category[];onChange:(f:TicketFilters)=>void})=>{const field='rounded-lg border border-slate-300 bg-white px-3 py-2 text-sm';return <div className="grid gap-3 rounded-xl border border-slate-200 bg-white p-4 shadow-sm sm:grid-cols-2 lg:grid-cols-6"><input aria-label="Search tickets" placeholder="Search tickets…" className={`${field} lg:col-span-2`} value={filters.search??''} onChange={e=>onChange({...filters,search:e.target.value||undefined})}/><select aria-label="Status" className={field} value={filters.status??''} onChange={e=>onChange({...filters,status:e.target.value||undefined})}><option value="">All statuses</option>{['new','open','assigned','in_progress','waiting_customer','escalated','resolved','closed','cancelled'].map(x=><option key={x} value={x}>{x.replaceAll('_',' ')}</option>)}</select><select aria-label="Priority" className={field} value={filters.priority??''} onChange={e=>onChange({...filters,priority:e.target.value||undefined})}><option value="">All priorities</option>{['low','normal','high','urgent'].map(x=><option key={x}>{x}</option>)}</select><select aria-label="Category" className={field} value={filters.category_id??''} onChange={e=>onChange({...filters,category_id:e.target.value||undefined})}><option value="">All categories</option>{categories.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><select aria-label="SLA state" className={field} value={filters.sla??''} onChange={e=>onChange({...filters,sla:e.target.value||undefined})}><option value="">All SLA states</option><option value="overdue">Overdue</option><option value="breached">Breached</option></select></div>};
